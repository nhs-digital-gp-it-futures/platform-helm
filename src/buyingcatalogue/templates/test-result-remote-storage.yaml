{{- if (eq .Values.localTestResultStorage.enabled false) -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bc-test-result-remote-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azurefile
  resources:
    requests:
      storage: 1Gi
{{- end }}