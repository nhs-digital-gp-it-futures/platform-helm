apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "email.fullname" . }}-test-connection"
  labels:
    {{- include "email.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "email.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
