apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "isapi-db.fullname" . }}-test-connection"
  labels:
    {{- include "isapi-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "isapi-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
